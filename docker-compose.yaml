services:
  serveur:
    platform: "linux/amd64"
    build:
      context: .
    ports:
      - "${SERVEUR_HTTP_PORT}:3000"
    volumes:
      - /serveur/node_modules
    command: npm run start
    env_file:
      - .env