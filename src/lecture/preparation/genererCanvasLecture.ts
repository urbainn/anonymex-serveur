
import { Canvas } from "canvas";

/**
 * Retourne un canvas prêt à la lecture.
 * @param inputPath Chemin acceptant les formats : PDF (pluieurs pages), PNG et JPG. Accepte aussi les archives (ZIP, RAR, 7Z) de ces formats.
 */
export default function genererCanvasLecture(inputPath: string) {

}

// 